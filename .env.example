# apps/web/.env.local
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
NEXT_PUBLIC_API_URL=http://localhost:4000

# apps/api/.env
# apps/api is not built yet (planned in a later phase, see README.md).
# These vars are a placeholder for when that service exists.
PORT=4000
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
